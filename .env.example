# Comma-separated list of domains you want to allow through the proxy.
# Supports exact domains and subdomains. Example: example.com,api.github.com
PROXY_ALLOWLIST=example.com

# Optional comma-separated list of frontend origins allowed for CORS.
# Leave empty to allow all origins.
CORS_ORIGINS=

# Optional rate limiting settings for /proxy
RATE_WINDOW_MS=60000
RATE_MAX=60

# Runtime port (set automatically on Railway/Replit)
PORT=3000
